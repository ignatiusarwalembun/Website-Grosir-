import { useEffect,useState } from 'react';import { Link,useParams } from 'react-router-dom';import { api } from '../services/api';import { rupiah,ProductCard } from '../components/Cards';import { Minus,Plus,ShoppingCart,PackageCheck,Truck,ChevronLeft } from 'lucide-react';import { useCart } from '../context/CartContext';
export default function ProductDetail(){const {id}=useParams();const [p,setP]=useState(null),[similar,setSimilar]=useState([]),[qty,setQty]=useState(1);const {add}=useCart();useEffect(()=>{api.product(id).then(x=>{setP(x);setQty(x.minOrder);const recent=JSON.parse(localStorage.getItem('grosirhub-recent')||'[]').filter(r=>r.id!==x.id);localStorage.setItem('grosirhub-recent',JSON.stringify([x,...recent].slice(0,6)));return api.products(`category=${x.category}`)}).then(x=>setSimilar(x.filter(a=>a.id!==id).slice(0,4)))},[id]);if(!p)return <div className="page detailLoading">Memuat detail produk...</div>;return <div className="page detailPage"><Link to="/belanja" className="backLink"><ChevronLeft/> Kembali ke belanja</Link><section className="detailTop"><div className="detailVisual"><span>{p.emoji}</span><small>{p.tag}</small></div><div className="detailInfo"><span className="eyebrow">{p.tag} · STOK {p.stock}</span><h1>{p.name}</h1><p>{p.description}</p><del>{rupiah(p.price)}</del><strong>{rupiah(p.wholesalePrice)}</strong><small>Harga mulai dari tier pembelian terbesar</small><div className="tiers"><b>Harga Grosir</b>{p.tiers.map((t,i)=><div key={i}><span>{t.min}{t.max?` - ${t.max}`:'+'} pcs</span><strong>{rupiah(t.price)}</strong></div>)}</div><div className="qtyRow"><div className="qty"><button onClick={()=>setQty(Math.max(p.minOrder,qty-1))}><Minus/></button><b>{qty}</b><button onClick={()=>setQty(qty+1)}><Plus/></button></div><button className="primaryBtn" onClick={()=>add(p,qty)}><ShoppingCart/> + Keranjang</button></div><div className="detailNotes"><span><PackageCheck/> Minimal order {p.minOrder} pcs</span><span><Truck/> Siap diproses dari gudang</span></div></div></section><section className="infoCards"><div><h3>Deskripsi Produk</h3><p>{p.description}</p></div><div><h3>Informasi Kemasan</h3><p>{p.packaging}. Cocok untuk stok usaha dan pembelian berulang.</p></div></section><section><div className="sectionTitle"><div><span>Masih satu kategori</span><h2>Produk Serupa</h2></div></div><div className="productGrid">{similar.map(x=><ProductCard p={x} key={x.id}/>)}</div></section></div>}
